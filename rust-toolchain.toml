[toolchain]
# Keep downstream qualification reproducible. Rust 1.98 enables a new
# `result_large_err` lint under `-D warnings` for untouched v0.43 APIs.
channel = "1.97.0"
components = ["clippy", "rustfmt"]
